<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.android.bakingapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="com.example.android.bakingapp.dagger.MyApplication"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/application_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ThemeWithActionBar">
        <activity
            android:name=".ui.activity.RecipeActivity"
            android:label="@string/application_name"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.RecipeDetailActivity"
            android:label="@string/recipe_detail_activity_label"
            android:launchMode="singleTop"
            android:parentActivityName=".ui.activity.RecipeActivity" />
        <activity
            android:name=".ui.activity.StepDetailActivity"
            android:label="@string/step_detail_activity_label"
            android:launchMode="singleTop"
            android:parentActivityName=".ui.activity.RecipeDetailActivity" />
    </application>
</manifest>